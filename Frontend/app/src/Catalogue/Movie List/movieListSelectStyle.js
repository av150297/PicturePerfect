const style = (theme) => {
  return {
    formControl: {
      margin: theme.spacing(1),
      minWidth: 120,
      width: "30%",
    },
    selectEmpty: {
      marginTop: theme.spacing(2),
    },
    select: {
      width: "100%",
    },
  };
};

export default style;
